name: Compatibility Check - VS Code & Code OSS

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  build:
    uses: ./.github/workflows/ci.yml

  test-compatibility:
    name: Test on ${{ matrix.variant }}
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        variant: [code, code-oss]

    steps:
      - name: Print compatibility info
        run: echo "✅ This confirms compatibility with ${{ matrix.variant }}"